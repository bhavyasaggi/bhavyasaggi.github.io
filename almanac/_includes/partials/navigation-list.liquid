{%- if nav_items and nav_items.size > 0 -%}
  <ol class="reset-box reset-list stack p-1">
    {%- for nav_item in nav_items -%}
      <li class="p-1">
        <a href="{{- nav_item.url -}}">
          {{- nav_item.title -}}
        </a>
      </li>
    {%- endfor -%}
  </ol>
  <br/>
{%- endif -%}
